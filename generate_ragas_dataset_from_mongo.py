"""
สคริปต์สำหรับสร้าง RAGAS Dataset จากข้อมูลใน MongoDB

สคริปต์นี้จะใช้ workflow ใหม่:
1. ให้ LLM สร้างคำถามเกี่ยวกับโหราศาสตร์ที่มีวันเกิดและเวลาเกิดจริงๆ (DD/MM/YYYY และ HH:MM)
2. ใช้ embedding model เพื่อ retrieve ข้อมูลจาก MongoDB astrobot_summary
3. ใช้ระบบ RAG จริงๆ (ask_question_to_rag) เพื่อสร้างคำตอบจากข้อมูลที่ retrieve มา
4. บันทึกเป็น dataset สำหรับ RAGAS evaluation

**สำคัญ**: 
- ไม่จำกัดจำนวนเอกสารใน collection (ดึงทั้งหมดที่มี embeddings)
- ใช้ embedding model เพื่อ retrieve ข้อมูล (เหมือน ask_question_to_rag)
- ใช้ระบบ RAG จริงๆ (ask_question_to_rag) แทนการสร้างคำตอบเอง
- ระบบ RAG จะทำการ retrieve ข้อมูลเองและสร้างคำตอบจากข้อมูลที่ retrieve มา
- **ทุกคำถามต้องมีวันเกิดและเวลาเกิดจริงๆ** เพื่อให้ระบบ RAG สามารถสร้างคำตอบได้
"""

import os
import json
import sys
import random
from typing import List, Dict, Any, Optional
from dotenv import load_dotenv
from datetime import datetime, date
from pymongo import MongoClient
import logging
import numpy as np

# โหลด environment variables
load_dotenv()

# ตั้งค่า logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# สุ่มวันเกิดเป็น DD/MM/YYYY (ใช้เมื่อไม่ได้กำหนดผ่าน env)
def generate_random_birth_date(
    start_year: int = 1950,
    end_year: int = 2010,
) -> str:
    """สุ่มวันเกิดในรูปแบบ DD/MM/YYYY"""
    year = random.randint(start_year, end_year)
    month = random.randint(1, 12)
    # จำนวนวันของแต่ละเดือน (ตรวจปีอธิกสุรทินสำหรับกุมภาพันธ์)
    days_in_month = [
        31,
        29 if (year % 400 == 0 or (year % 4 == 0 and year % 100 != 0)) else 28,
        31, 30, 31, 30, 31, 31, 30, 31, 30, 31,
    ]
    day = random.randint(1, days_in_month[month - 1])
    return f"{day:02d}/{month:02d}/{year:04d}"

# Import config
from config import SUMMARY_DB_NAME

# Import OpenAI
try:
    from openai import OpenAI
except ImportError:
    logger.error("❌ ไม่พบ OpenAI library. กรุณาติดตั้งด้วย: pip install openai")
    sys.exit(1)


def get_mongodb_data() -> List[Dict[str, Any]]:
    """
    ดึงข้อมูลจาก MongoDB collections ใน astrobot_summary ที่มี embeddings แล้วเท่านั้น
    (ไม่จำกัดจำนวน)
    
    Returns:
        List[Dict]: รายการข้อมูลจาก MongoDB ที่มี embeddings แล้ว
    """
    mongo_uri = os.getenv("MONGO_URL")
    if not mongo_uri or mongo_uri == "mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority":
        logger.error("❌ MONGO_URL ไม่ได้ตั้งค่าหรือยังเป็นค่า default")
        return []
    
    try:
        client = MongoClient(mongo_uri, serverSelectionTimeoutMS=10000)
        db = client[SUMMARY_DB_NAME]
        
        collections_to_search = [
            "processed_text_chunks",
            "processed_image_chunks",
            "processed_table_chunks",
        ]
        
        all_data = []
        
        for collection_name in collections_to_search:
            try:
                collection = db[collection_name]
                
                # ✅ กรองเฉพาะเอกสารที่มี embeddings field และ embeddings ไม่เป็น None
                query = {
                    "embeddings": {"$exists": True, "$ne": None}
                }
                
                doc_count_with_embeddings = collection.count_documents(query)
                
                if doc_count_with_embeddings == 0:
                    logger.warning(f"⚠️ Collection {collection_name} ไม่มีข้อมูลที่มี embeddings")
                    continue
                
                # ✅ ดึงเฉพาะเอกสารที่มี embeddings (ไม่จำกัดจำนวน)
                docs = list(collection.find(query))
                
                docs_with_content = 0
                for doc in docs:
                    # ✅ ใช้ summary ก่อน ถ้าไม่มีใช้ text
                    text_content = doc.get("summary") or doc.get("text", "")
                    
                    # ✅ ตรวจสอบว่ามี embeddings และเป็น list/array ที่ไม่ว่าง
                    embeddings = doc.get("embeddings")
                    if not embeddings or (isinstance(embeddings, list) and len(embeddings) == 0):
                        logger.warning(f"⚠️ เอกสาร chunk_id={doc.get('chunk_id', 'unknown')} มี embeddings field แต่เป็นค่าว่าง - ข้าม")
                        continue
                    
                    if text_content and text_content.strip():
                        # ตรวจสอบว่าเนื้อหามาจาก MongoDB จริงๆ
                        doc_source = doc.get("source", "unknown")
                        doc_page = doc.get("page", None)
                        doc_chunk_id = doc.get("chunk_id", 0)
                        
                        all_data.append({
                            "content": text_content.strip(),
                            "type": collection_name.replace("processed_", "").replace("_chunks", ""),
                            "source": doc_source,
                            "chunk_id": doc_chunk_id,
                            "page": doc_page,  # ✅ เพิ่ม page number
                            "embeddings": embeddings,  # ✅ เก็บ embeddings เพื่อคำนวณ similarity
                            "_mongodb_id": str(doc.get("_id", "")),  # ✅ เก็บ MongoDB _id เพื่อตรวจสอบ
                        })
                        docs_with_content += 1
                        
                        # Log ข้อมูล source สำหรับ debugging
                        if docs_with_content <= 3:  # แสดง 3 ตัวอย่างแรก
                            logger.debug(f"   📄 ตัวอย่าง: page={doc_page}, source={doc_source}, chunk_id={doc_chunk_id}, content_length={len(text_content)}")
                
                logger.info(f"✅ ดึงข้อมูลจาก {collection_name}: {docs_with_content}/{len(docs)} เอกสารที่มี embeddings และเนื้อหา")
                
            except Exception as e:
                logger.warning(f"⚠️ Error reading from {collection_name}: {e}")
                continue
        
        client.close()
        
        logger.info(f"✅ ดึงข้อมูลทั้งหมด: {len(all_data)} chunks (เฉพาะที่มี embeddings แล้ว)")
        return all_data
        
    except Exception as e:
        logger.error(f"❌ Error connecting to MongoDB: {e}")
        import traceback
        traceback.print_exc()
        return []


def generate_qa_pairs_with_llm(
    content: str,
    content_type: str,
    openai_client: OpenAI,
    num_questions: int = 2,
    invalid_answer_phrases: List[str] = None
) -> List[Dict[str, str]]:
    """
    ใช้ LLM สร้างคำถามและคำตอบจากเนื้อหา
    
    Args:
        content: เนื้อหาที่จะสร้างคำถาม-คำตอบ
        content_type: ประเภทเนื้อหา (text/image/table)
        openai_client: OpenAI client
        num_questions: จำนวนคำถามที่ต้องการสร้างต่อเนื้อหา
        
    Returns:
        List[Dict]: รายการของคำถาม-คำตอบ
    """
    try:
        # จำกัดความยาวของเนื้อหาเพื่อไม่ให้ prompt ยาวเกินไป
        max_content_length = 2000
        truncated_content = content[:max_content_length]
        if len(content) > max_content_length:
            truncated_content += "..."
        
        # สุ่มวันเกิดและเวลาเกิดที่หลากหลายสำหรับแต่ละ chunk เพื่อให้ได้คำถามที่หลากหลาย
        # สุ่มวันเกิดใหม่ทุกครั้งเพื่อหลีกเลี่ยงคำถามซ้ำ
        birth_date = generate_random_birth_date()
        
        # สุ่มวันเกิดเพิ่มเติมเพื่อให้ได้คำถามที่หลากหลายมากขึ้น
        additional_dates = [generate_random_birth_date() for _ in range(3)]
        
        # สุ่มเวลาเกิด (HH:MM)
        def generate_random_birth_time():
            hour = random.randint(0, 23)
            minute = random.choice([0, 15, 30, 45])
            return f"{hour:02d}:{minute:02d}"
        
        birth_times = [generate_random_birth_time() for _ in range(3)]
        
        birth_info = f"""วันเกิดและเวลาเกิดตัวอย่างสำหรับสร้างคำถาม (ใช้เป็นแนวทาง):
- วันเกิดหลัก: {birth_date} เวลา {birth_times[0]} น.
- วันเกิดเพิ่มเติม: {additional_dates[0]} เวลา {birth_times[1]} น., {additional_dates[1]} เวลา {birth_times[2]} น., {additional_dates[2]}

**สำคัญ: สร้างคำถามที่ใช้วันเกิดและเวลาเกิดจริงๆ หลากหลาย ไม่ใช่คำถามทั่วไปที่ซ้ำกัน**
"""

        prompt = f"""คุณเป็นผู้เชี่ยวชาญด้านโหราศาสตร์และระบบแชทบอทโหราศาสตร์

**⚠️ สำคัญ: ข้อมูลที่ให้มาด้านล่างนี้มาจาก MongoDB database (astrobot_summary) เท่านั้น**

{birth_info}กรุณาพิจารณาวันเกิด (ถ้ามีให้มา) เป็นบริบทตั้งต้นก่อน แล้วจึงสร้างคำถาม-คำตอบจากเนื้อหาด้านล่าง

**📋 ข้อกำหนดสำคัญ:**
1. **ใช้เฉพาะข้อมูลจากเนื้อหาที่ให้มาเท่านั้น** - ห้ามใช้ข้อมูลจากภายนอกหรือความรู้ทั่วไป
2. **ข้อมูลมาจาก MongoDB** - เนื้อหาด้านล่างนี้ถูกดึงมาจาก MongoDB collection (processed_text_chunks, processed_image_chunks, หรือ processed_table_chunks)
3. **คำตอบต้องมาจากเนื้อหา** - คำตอบทุกข้อต้องอ้างอิงจากเนื้อหาที่ให้มาเท่านั้น
4. **ห้ามเพิ่มเติมข้อมูล** - ถ้าเนื้อหาไม่มีข้อมูลบางอย่าง ห้ามเติมข้อมูลจากความรู้ทั่วไป

จากเนื้อหาด้านล่าง (ที่มาจาก MongoDB) กรุณาสร้างคำถามและคำตอบที่**เกี่ยวข้องกับวันเดือนปีเกิดและเวลาเกิด**เป็นหลัก โดยเน้นหัวข้อต่อไปนี้:
- **วันเกิดและราศี** - การคำนวณราศีจากวันเกิด, ราศีประจำวันเกิด, ช่วงวันที่ของแต่ละราศี
- **เวลาเกิดและลัคณา** - การคำนวณลัคณา (Ascendant) จากวันเกิดและเวลาเกิด, ราศีประจำลัคนา
- **การคำนวณดวงชะตาจากวันเกิดและเวลาเกิด** - การใช้วันเกิดและเวลาเกิดในการคำนวณดวงชะตา, บ้านทั้ง 12 บ้าน
- **ราศีทั้ง 12 ราศี** (เมษ, พฤษภ, เมถุน, กรกฎ, สิงห์, กันย์, ตุล, พิจิก, ธนู, มังกร, กุมภ์, มีน) - ในบริบทของวันเกิด
- **ลัคณา (Ascendant) และราศีประจำลัคนา** - การคำนวณจากวันเกิดและเวลาเกิด, ความสำคัญของเวลาเกิดในการคำนวณลัคนา
- **บ้านทั้ง 12 บ้านในดวงชะตา** - การคำนวณจากวันเกิดและเวลาเกิด
- **ดวงชะตาและการทำนาย** - การใช้วันเกิดและเวลาเกิดในการทำนาย
- **ลักษณะนิสัยของแต่ละราศี** - ในบริบทของวันเกิด
- **ธาตุและคุณภาพของราศี** - ในบริบทของวันเกิด
- **ข้อมูลโหราศาสตร์ที่เกี่ยวข้องกับวันเกิดและเวลาเกิด** - การคำนวณ, การตีความ, การทำนายจากวันเกิดและเวลาเกิด

**เนื้อหาจาก MongoDB:**
{truncated_content}

**ประเภทเนื้อหา:** {content_type}

**คำแนะนำ:**
1. **สร้างคำถามที่หลากหลายและใช้วันเกิดจริงๆ** - ใช้วันเกิดที่ให้มาเป็นตัวอย่าง หรือสุ่มวันเกิดใหม่ที่หลากหลาย
2. **ห้ามสร้างคำถามซ้ำกัน** - แต่ละคำถามต้องแตกต่างกันและใช้วันเกิดที่แตกต่างกัน
3. **ตัวอย่างคำถามที่หลากหลาย (เน้นทั้งวันเกิดและเวลาเกิด):**
   - "ราศีของผู้ที่เกิดวันที่ {birth_date} เวลา {birth_times[0]} น. คืออะไร?"
   - "วันเกิด {additional_dates[0]} เวลา {birth_times[1]} น. ตรงกับราศีอะไร?"
   - "การคำนวณดวงชะตาจากวันเกิด {additional_dates[1]} เวลา {birth_times[2]} น. ทำอย่างไร?"
   - "ลัคณาของผู้เกิดวันที่ {birth_date} เวลา {birth_times[0]} น. คืออะไร?"
   - "ผู้ที่เกิดวันที่ {additional_dates[0]} เวลา {birth_times[1]} น. มีราศีอะไรและมีลักษณะนิสัยอย่างไร?"
   - "บ้านทั้ง 12 บ้านในดวงชะตาของผู้เกิดวันที่ {birth_date} เวลา {birth_times[0]} น. คืออะไร?"
   - "การทำนายดวงชะตาจากวันเกิด {additional_dates[1]} เวลา {birth_times[2]} น. ทำอย่างไร?"
   - "ธาตุและคุณภาพของราศีสำหรับผู้เกิดวันที่ {birth_date} เวลา {birth_times[0]} น. คืออะไร?"
   - "ความสำคัญของเวลาเกิดในการคำนวณลัคณาคืออะไร?"
   - "การคำนวณราศีประจำลัคนาจากวันเกิด {additional_dates[2]} เวลา {birth_times[1]} น. ทำอย่างไร?"
   - "ผู้เกิดวันที่ {birth_date} เวลา {birth_times[2]} น. มีลัคณาอะไร?"
   - "การใช้วันเกิดและเวลาเกิดในการทำนายดวงชะตาทำอย่างไร?"
4. **สร้างคำถามที่หลากหลาย** - ใช้วันเกิดที่แตกต่างกัน, เวลาเกิดที่แตกต่างกัน (เช่น 08:00, 14:30, 22:15), และมุมมองที่แตกต่างกัน
5. **เน้นคำถามที่รวมทั้งวันเกิดและเวลาเกิด** - อย่างน้อย 70% ของคำถามควรมีทั้งวันเกิดและเวลาเกิด
6. **คำตอบต้องมาจากเนื้อหาจาก MongoDB เท่านั้น** - ห้ามใช้ข้อมูลจากภายนอก
7. **ห้ามตอบว่า "ไม่มีในเนื้อหา", "ไม่มีข้อมูล", "ไม่ได้ถูกระบุไว้ในเนื้อหา", "ไม่ปรากฏในเนื้อหา", "ไม่สามารถระบุ", "ไม่สามารถระบุได้", "ไม่สามารถระบุจากข้อมูลที่มีอยู่" หรือวลีที่คล้ายกัน** - ให้ใช้ข้อมูลที่มีอยู่จริงในเนื้อหาจาก MongoDB เท่านั้น
8. **ถ้าเนื้อหาไม่มีข้อมูลเกี่ยวกับวันเกิดเฉพาะเจาะจง** ให้สร้างคำถามทั่วไปที่สามารถตอบได้จากเนื้อหาที่มี เช่น:
   - "ราศีกรกฎมีลักษณะนิสัยอย่างไร?" (แทนที่จะถาม "ราศีของผู้ที่เกิดวันที่ XX/XX/XXXX คืออะไร?" ถ้าเนื้อหาไม่มีข้อมูลเกี่ยวกับวันเกิดนั้น)
   - "การคำนวณราศีจากวันเกิดทำอย่างไร?" (แทนที่จะถาม "การคำนวณราศีจากวันเกิด XX/XX/XXXX ทำอย่างไร?" ถ้าเนื้อหาไม่มีข้อมูลเฉพาะเจาะจง)
   - "ลัคณาคืออะไรและคำนวณอย่างไร?" (แทนที่จะถาม "ลัคณาของผู้เกิดวันที่ XX/XX/XXXX เวลา XX:XX น. คืออะไร?" ถ้าเนื้อหาไม่มีข้อมูลเฉพาะเจาะจง)
9. **contexts ต้องเป็นประโยคที่มาจากเนื้อหาจริงๆ** - อย่าปล่อยให้ว่างเปล่า
10. สร้าง {num_questions} คำถาม-คำตอบคู่ที่หลากหลายและไม่ซ้ำกัน
11. ใช้ภาษาไทยเท่านั้น
12. **ใช้ข้อมูลทุกประเภท** - text, image (OCR), table - ที่มาจาก MongoDB

**รูปแบบผลลัพธ์ (JSON):**
[
  {{
    "question": "คำถามที่เกี่ยวข้องกับเนื้อหาจาก MongoDB",
    "answer": "คำตอบที่มาจากเนื้อหาจาก MongoDB เท่านั้น (ห้ามใช้ข้อมูลจากภายนอก)",
    "contexts": ["ประโยคสำคัญจากเนื้อหาที่เกี่ยวข้องที่ใช้ในการตอบคำถาม"]
  }}
]

**⚠️ ข้อกำหนดสำคัญสำหรับ contexts:**
- contexts ต้องเป็นประโยคหรือข้อความที่มาจากเนื้อหาที่ให้มาจริงๆ
- contexts ต้องเกี่ยวข้องกับคำถามและคำตอบ
- contexts ต้องไม่ว่างเปล่า
- ถ้าเนื้อหาไม่มีข้อมูลเกี่ยวกับวันเกิดเฉพาะเจาะจง ให้สร้างคำถามที่สามารถตอบได้จากเนื้อหาทั่วไปที่มี (เช่น ข้อมูลทั่วไปเกี่ยวกับราศี, การคำนวณดวงชะตา)
- **ห้ามตอบว่า "ไม่มีในเนื้อหา", "ไม่มีข้อมูล", "ไม่ได้ถูกระบุไว้ในเนื้อหา", "ไม่ปรากฏในเนื้อหา", "ไม่สามารถระบุ", "ไม่สามารถระบุได้", "ไม่สามารถระบุจากข้อมูลที่มีอยู่" หรือวลีที่คล้ายกัน** - ให้ใช้ข้อมูลที่มีอยู่จริงในเนื้อหาจาก MongoDB เท่านั้น
- **ถ้าเนื้อหาไม่มีข้อมูลเฉพาะเจาะจง** ให้สร้างคำถามทั่วไปที่สามารถตอบได้จากเนื้อหาที่มี (เช่น "ราศีกรกฎมีลักษณะนิสัยอย่างไร?" แทนที่จะถามเกี่ยวกับวันเกิดเฉพาะ)

**⚠️ จำไว้: ใช้เฉพาะข้อมูลจากเนื้อหาที่ให้มา (จาก MongoDB) เท่านั้น ห้ามใช้ข้อมูลจากภายนอกหรือความรู้ทั่วไป**

กรุณาสร้างคำถาม-คำตอบในรูปแบบ JSON เท่านั้น ไม่ต้องมีข้อความอื่น:"""

        # เรียกใช้ OpenAI
        openai_model = os.getenv("OPENAI_MODEL", "gpt-4o-mini")
        
        response = openai_client.chat.completions.create(
            model=openai_model,
            messages=[
                {"role": "system", "content": "คุณเป็นผู้เชี่ยวชาญด้านโหราศาสตร์และระบบแชทบอทโหราศาสตร์ สร้างคำถาม-คำตอบในรูปแบบ JSON เท่านั้น ใช้เฉพาะข้อมูลจากเนื้อหาที่ให้มา (จาก MongoDB) เท่านั้น ห้ามใช้ข้อมูลจากภายนอกหรือความรู้ทั่วไป"},
                {"role": "user", "content": prompt}
            ],
            temperature=0.7,
            max_tokens=1500,
        )
        
        response_text = response.choices[0].message.content.strip()
        
        # ลบ markdown code blocks ถ้ามี
        if response_text.startswith("```json"):
            response_text = response_text[7:]
        if response_text.startswith("```"):
            response_text = response_text[3:]
        if response_text.endswith("```"):
            response_text = response_text[:-3]
        response_text = response_text.strip()
        
        # Parse JSON
        try:
            qa_pairs = json.loads(response_text)
            
            # ตรวจสอบว่าเป็น list
            if not isinstance(qa_pairs, list):
                qa_pairs = [qa_pairs]
            
            # ตรวจสอบโครงสร้าง
            valid_pairs = []
            for pair in qa_pairs:
                if isinstance(pair, dict) and "question" in pair and "answer" in pair:
                    # ตรวจสอบว่าคำตอบไม่ใช่ "ไม่มีในเนื้อหา" หรือ "ไม่ได้ถูกระบุไว้ในเนื้อหา"
                    answer = str(pair.get("answer", "")).strip()
                    # ใช้ invalid_answer_phrases ที่ส่งมา หรือใช้ default
                    check_phrases = invalid_answer_phrases if invalid_answer_phrases else [
                        "ไม่มีในเนื้อหา", "ไม่มีให้ในเนื้อหา", "ไม่มีข้อมูล", "ไม่พบข้อมูล",
                        "ไม่ได้ถูกระบุไว้ในเนื้อหา", "ไม่ได้ระบุในเนื้อหา", "ไม่ได้ถูกระบุในเนื้อหา",
                        "ไม่ปรากฏในเนื้อหา", "ยังไม่มีการระบุ", "ไม่มีการระบุ", "ไม่ได้ระบุไว้",
                        "ไม่พบในเนื้อหา", "ไม่มีให้ในเนื้อหาที่ให้มา", "ไม่ได้ระบุไว้ในเนื้อหาที่ให้มา",
                        "ไม่สามารถให้ได้จากเนื้อหาที่มีอยู่", "ยังไม่ได้มีการระบุ", "ไม่สามารถให้ได้",
                        "ไม่สามารถระบุ", "ไม่สามารถระบุได้", "ไม่สามารถระบุจาก", "ไม่สามารถระบุจากข้อมูล",
                        "ไม่สามารถระบุจากข้อมูลที่มีอยู่", "ไม่สามารถระบุการคำนวณ", "ไม่สามารถระบุราศี",
                        "ไม่สามารถระบุลัคณา", "ไม่สามารถระบุการคำนวณลัคณา", "ไม่สามารถระบุการคำนวณราศี"
                    ]
                    if any(phrase in answer for phrase in check_phrases):
                        logger.warning(f"⚠️ ข้ามคำถามที่ LLM ตอบว่าไม่มีข้อมูล: {pair.get('question', '')[:50]}...")
                        logger.warning(f"   คำตอบ: {answer[:100]}...")
                        continue
                    
                    # ตรวจสอบและเพิ่ม contexts
                    if "contexts" not in pair or not pair["contexts"]:
                        # ใช้เนื้อหาทั้งหมดเป็น context ถ้า LLM ไม่ให้มา
                        pair["contexts"] = [truncated_content[:500]]
                    else:
                        # ตรวจสอบว่า contexts ไม่ว่างเปล่า
                        pair["contexts"] = [ctx for ctx in pair["contexts"] if ctx and ctx.strip()]
                        if not pair["contexts"]:
                            # ถ้า contexts ว่างเปล่า ให้ใช้เนื้อหาทั้งหมด
                            pair["contexts"] = [truncated_content[:500]]
                    
                    valid_pairs.append(pair)
            
            return valid_pairs
            
        except json.JSONDecodeError as e:
            logger.warning(f"⚠️ Error parsing JSON response: {e}")
            logger.warning(f"Response text: {response_text[:200]}...")
            return []
        
    except Exception as e:
        logger.warning(f"⚠️ Error generating QA pairs: {e}")
        import traceback
        traceback.print_exc()
        return []


def retrieve_from_mongodb_with_embedding(
    question: str,
    embedding_model,
    top_k: int = 5,
    threshold: float = 0.10
) -> List[Dict[str, Any]]:
    """
    Retrieve ข้อมูลจาก MongoDB โดยใช้ embedding model (เหมือน ask_question_to_rag)
    
    Args:
        question: คำถาม
        embedding_model: SentenceTransformer model
        top_k: จำนวนเอกสารที่ต้องการดึง
        threshold: threshold สำหรับ similarity score
        
    Returns:
        List[Dict]: รายการเอกสารที่ retrieve มา พร้อม similarity score
    """
    try:
        import numpy as np
        from pymongo import MongoClient
        
        mongo_uri = os.getenv("MONGO_URL")
        if not mongo_uri or mongo_uri == "mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority":
            logger.error("❌ MONGO_URL ไม่ได้ตั้งค่าหรือยังเป็นค่า default")
            return []
        
        # สร้าง query embedding
        query_embedding = embedding_model.encode(question, convert_to_numpy=True)
        logger.debug(f"✅ สร้าง query embedding สำเร็จ (ขนาด: {len(query_embedding)} dimensions)")
        
        # เชื่อมต่อ MongoDB
        client = MongoClient(mongo_uri, serverSelectionTimeoutMS=10000)
        db = client[SUMMARY_DB_NAME]
        
        collections_to_search = [
            "processed_text_chunks",
            "processed_image_chunks",
            "processed_table_chunks",
        ]
        
        all_retrieved_docs = []
        
        for collection_name in collections_to_search:
            try:
                collection = db[collection_name]
                
                # ดึงข้อมูลทั้งหมดที่มี embeddings
                query = {
                    "embeddings": {"$exists": True, "$ne": None}
                }
                docs = list(collection.find(query))
                
                if not docs:
                    continue
                
                logger.debug(f"📂 กำลังค้นหาใน collection: {collection_name} ({len(docs)} เอกสาร)")
                
                # คำนวณ similarity scores
                similarities = []
                for doc in docs:
                    if 'embeddings' not in doc:
                        continue
                    
                    try:
                        doc_embedding = np.array(doc['embeddings'])
                        
                        # ตรวจสอบว่า dimensions ตรงกัน
                        if len(doc_embedding) != len(query_embedding):
                            continue
                        
                        # Cosine similarity
                        similarity = np.dot(query_embedding, doc_embedding) / (
                            np.linalg.norm(query_embedding) * np.linalg.norm(doc_embedding)
                        )
                        similarities.append((similarity, doc))
                    except Exception as e:
                        logger.debug(f"⚠️ Error calculating similarity: {e}")
                        continue
                
                # เรียงตาม similarity score
                similarities.sort(key=lambda x: x[0], reverse=True)
                
                # เอาข้อมูลที่มี similarity สูงสุด top_k อันดับแรก
                top_docs = similarities[:top_k]
                
                for similarity, doc in top_docs:
                    if similarity < threshold:
                        continue
                    
                    # ใช้ summary ก่อน ถ้าไม่มีใช้ text
                    text_content = doc.get("summary") or doc.get("text", "")
                    
                    if not text_content or not text_content.strip():
                        continue
                    
                    source_info = doc.get("source", "unknown")
                    page_number = doc.get("page", None)
                    chunk_id = doc.get("chunk_id", 0)
                    
                    all_retrieved_docs.append({
                        "content": text_content.strip(),
                        "similarity": float(similarity),
                        "source": source_info,
                        "page": page_number,
                        "chunk_id": chunk_id,
                        "collection": collection_name,
                        "_mongodb_id": str(doc.get("_id", "")),
                    })
                
            except Exception as e:
                logger.warning(f"⚠️ Error reading from {collection_name}: {e}")
                continue
        
        client.close()
        
        # เรียงตาม similarity อีกครั้ง (รวมจากทุก collection)
        all_retrieved_docs.sort(key=lambda x: x["similarity"], reverse=True)
        
        # เอาสูงสุด top_k
        all_retrieved_docs = all_retrieved_docs[:top_k]
        
        logger.debug(f"✅ Retrieve ได้ {len(all_retrieved_docs)} เอกสาร")
        return all_retrieved_docs
        
    except Exception as e:
        logger.error(f"❌ Error retrieving from MongoDB: {e}")
        import traceback
        traceback.print_exc()
        return []


def generate_questions_with_llm(
    openai_client: OpenAI,
    num_questions: int = 10,
    topics: List[str] = None
) -> List[str]:
    """
    ให้ LLM สร้างคำถามเกี่ยวกับโหราศาสตร์ที่มีวันเกิดและเวลาเกิดจริงๆ
    
    **สำคัญ**: ทุกคำถามต้องมีวันเกิดและเวลาเกิดในรูปแบบ DD/MM/YYYY และ HH:MM
    เพื่อให้ระบบ RAG สามารถสร้างคำตอบได้
    
    Args:
        openai_client: OpenAI client
        num_questions: จำนวนคำถามที่ต้องการสร้าง
        topics: หัวข้อที่ต้องการให้สร้างคำถาม (ถ้า None จะใช้หัวข้อทั่วไป)
        
    Returns:
        List[str]: รายการคำถาม (ทุกคำถามมีวันเกิดและเวลาเกิด)
    """
    try:
        if topics is None:
            topics = [
                "วันเกิดและราศี",
                "เวลาเกิดและลัคณา",
                "การคำนวณดวงชะตา",
                "ราศีทั้ง 12 ราศี",
                "ลัคณา (Ascendant)",
                "บ้านทั้ง 12 บ้านในดวงชะตา",
                "ดวงชะตาและการทำนาย",
                "ลักษณะนิสัยของแต่ละราศี",
                "ธาตุและคุณภาพของราศี",
            ]
        
        topics_str = "\n".join([f"- {topic}" for topic in topics])
        
        # สร้างวันเกิดและเวลาเกิดตัวอย่างที่หลากหลาย
        import random
        def generate_random_birth_date():
            year = random.randint(1990, 2010)
            month = random.randint(1, 12)
            days_in_month = [31, 29 if (year % 400 == 0 or (year % 4 == 0 and year % 100 != 0)) else 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31]
            day = random.randint(1, days_in_month[month - 1])
            return f"{day:02d}/{month:02d}/{year:04d}"
        
        def generate_random_birth_time():
            hour = random.randint(0, 23)
            minute = random.choice([0, 15, 30, 45])
            return f"{hour:02d}:{minute:02d}"
        
        # สร้างตัวอย่างวันเกิดและเวลาเกิดหลายๆ ชุด
        example_dates = [generate_random_birth_date() for _ in range(5)]
        example_times = [generate_random_birth_time() for _ in range(5)]
        
        # สร้างตัวอย่างคำถาม
        example_questions = []
        for i in range(min(3, len(example_dates))):
            example_questions.append(f"- \"ทำนายดวง {example_dates[i]} เวลา {example_times[i]}\"")
            example_questions.append(f"- \"ราศีอะไร {example_dates[i]} เวลา {example_times[i]}\"")
            example_questions.append(f"- \"ลัคณา {example_dates[i]} เวลา {example_times[i]}\"")
        
        examples_str = "\n".join(example_questions)
        
        prompt = f"""คุณเป็นผู้เชี่ยวชาญด้านโหราศาสตร์

กรุณาสร้างคำถามเกี่ยวกับโหราศาสตร์ที่หลากหลาย โดยเน้นหัวข้อต่อไปนี้:
{topics_str}

**ข้อกำหนดสำคัญ:**
1. **ทุกคำถามต้องมีวันเกิดและเวลาเกิดจริงๆ** - ใช้รูปแบบ DD/MM/YYYY และ HH:MM
2. สร้างคำถามที่หลากหลายและไม่ซ้ำกัน
3. เน้นคำถามที่เกี่ยวข้องกับวันเกิด เวลาเกิด ราศี ลัคณา ดวงชะตา
4. ใช้ภาษาไทยเท่านั้น
5. สร้างคำถามที่สามารถตอบได้จากข้อมูลในฐานข้อมูลโหราศาสตร์
6. **ใช้วันเกิดและเวลาเกิดที่หลากหลาย** - ไม่ใช้วันเกิดเดียวกันซ้ำๆ

**รูปแบบวันเกิดและเวลาเกิด:**
- วันเกิด: DD/MM/YYYY (เช่น 07/09/2003, 15/08/1995, 20/12/2000)
- เวลาเกิด: HH:MM (เช่น 14:30, 10:00, 18:45, 22:15)

**ตัวอย่างคำถามที่ดี (ทุกคำถามต้องมีวันเกิดและเวลาเกิด):**
{examples_str}
- "ทำนายดวง 25/03/1998 เวลา 08:30"
- "ราศีอะไร 12/07/2005 เวลา 16:45"
- "ลัคณา 30/11/1992 เวลา 20:00"
- "ดวงชะตา 05/01/2001 เวลา 12:15"
- "บ้านทั้ง 12 บ้าน 18/06/1997 เวลา 14:30"

**ตัวอย่างคำถามที่ควรหลีกเลี่ยง:**
- "ลัคณาคืออะไร?" (ไม่มีวันเกิดและเวลาเกิด)
- "ราศีกรกฎมีลักษณะนิสัยอย่างไร?" (ไม่มีวันเกิดและเวลาเกิด)
- "การคำนวณดวงชะตาทำอย่างไร?" (ไม่มีวันเกิดและเวลาเกิด)

**รูปแบบผลลัพธ์ (JSON):**
{{
  "questions": [
    "คำถามที่ 1 (ต้องมีวันเกิดและเวลาเกิด)",
    "คำถามที่ 2 (ต้องมีวันเกิดและเวลาเกิด)",
    ...
  ]
}}

**สำคัญ: ทุกคำถามต้องมีวันเกิดและเวลาเกิดจริงๆ ในรูปแบบ DD/MM/YYYY และ HH:MM**

กรุณาสร้าง {num_questions} คำถามในรูปแบบ JSON เท่านั้น ไม่ต้องมีข้อความอื่น:"""
        
        openai_model = os.getenv("OPENAI_MODEL", "gpt-4o-mini")
        
        response = openai_client.chat.completions.create(
            model=openai_model,
            messages=[
                {"role": "system", "content": "คุณเป็นผู้เชี่ยวชาญด้านโหราศาสตร์ สร้างคำถามในรูปแบบ JSON เท่านั้น"},
                {"role": "user", "content": prompt}
            ],
            temperature=0.8,
            max_tokens=1000,
        )
        
        response_text = response.choices[0].message.content.strip()
        
        # ลบ markdown code blocks ถ้ามี
        if response_text.startswith("```json"):
            response_text = response_text[7:]
        if response_text.startswith("```"):
            response_text = response_text[3:]
        if response_text.endswith("```"):
            response_text = response_text[:-3]
        response_text = response_text.strip()
        
        # Parse JSON
        try:
            result = json.loads(response_text)
            questions = result.get("questions", [])
            
            if not isinstance(questions, list):
                questions = [questions]
            
            # กรองคำถามที่ว่างเปล่า
            questions = [q.strip() for q in questions if q and q.strip()]
            
            return questions
            
        except json.JSONDecodeError as e:
            logger.warning(f"⚠️ Error parsing JSON response: {e}")
            logger.warning(f"Response text: {response_text[:200]}...")
            return []
        
    except Exception as e:
        logger.warning(f"⚠️ Error generating questions: {e}")
        import traceback
        traceback.print_exc()
        return []


def generate_answer_from_retrieved_data(
    question: str,
    retrieved_docs: List[Dict[str, Any]],
    invalid_answer_phrases: List[str] = None
) -> Dict[str, Any]:
    """
    ใช้ระบบ RAG จริงๆ (ask_question_to_rag) เพื่อสร้างคำตอบจากข้อมูลที่ retrieve มา
    
    Args:
        question: คำถาม
        retrieved_docs: ข้อมูลที่ retrieve มา (ใช้เพื่อสร้าง contexts)
        invalid_answer_phrases: วลีที่บ่งชี้ว่าข้อมูลไม่มีในเนื้อหา
        
    Returns:
        Dict: คำตอบและ contexts
    """
    try:
        # Import ask_question_to_rag จาก retrieval_utils
        import sys
        import os
        sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'app'))
        from app.retrieval_utils import ask_question_to_rag
        
        if not retrieved_docs:
            return {
                "answer": "",
                "contexts": []
            }
        
        # ใช้ระบบ RAG จริงๆ (ask_question_to_rag)
        # ระบบนี้จะทำการ retrieve ข้อมูลเองและสร้างคำตอบจากข้อมูลที่ retrieve มา
        logger.debug(f"🔄 ใช้ระบบ RAG (ask_question_to_rag) เพื่อสร้างคำตอบ...")
        answer = ask_question_to_rag(question, user_id="dataset_generation")
        
        if not answer or not answer.strip():
            return {
                "answer": "",
                "contexts": []
            }
        
        # ตรวจสอบว่าคำตอบไม่บอกว่าไม่มีข้อมูล
        if invalid_answer_phrases:
            answer_lower = answer.lower()
            if any(phrase.lower() in answer_lower for phrase in invalid_answer_phrases):
                logger.warning(f"⚠️ ระบบ RAG ตอบว่าไม่มีข้อมูล: {answer[:80]}...")
                return {
                    "answer": "",
                    "contexts": []
                }
        
        # สร้าง contexts จาก retrieved_docs
        contexts = []
        for doc in retrieved_docs:
            content = doc.get("content", "")
            if content and content.strip():
                # ใช้เนื้อหาส่วนแรก (500 ตัวอักษร) เป็น context
                contexts.append(content[:500].strip())
        
        # ถ้าไม่มี contexts ให้ใช้เนื้อหาจาก retrieved docs
        if not contexts:
            contexts = [doc.get("content", "")[:500] for doc in retrieved_docs[:3] if doc.get("content", "").strip()]
        
        # กรอง contexts ที่ว่างเปล่า
        contexts = [ctx.strip() for ctx in contexts if ctx and ctx.strip()]
        
        return {
            "answer": answer.strip(),
            "contexts": contexts
        }
        
    except ImportError as e:
        logger.error(f"❌ ไม่สามารถ import ask_question_to_rag ได้: {e}")
        logger.error("   ตรวจสอบว่า app/retrieval_utils.py มีอยู่และมีฟังก์ชัน ask_question_to_rag")
        return {
            "answer": "",
            "contexts": []
        }
    except Exception as e:
        logger.warning(f"⚠️ Error generating answer with RAG: {e}")
        import traceback
        traceback.print_exc()
        return {
            "answer": "",
            "contexts": []
        }


def calculate_similarity(question: str, content_embeddings: List[float], embedding_model=None) -> float:
    """
    คำนวณ similarity ระหว่างคำถามกับ content โดยใช้ embeddings
    
    Args:
        question: คำถาม
        content_embeddings: embeddings ของ content
        embedding_model: SentenceTransformer model (ถ้า None จะสร้างใหม่)
        
    Returns:
        float: similarity score (0-1)
    """
    try:
        from sentence_transformers import SentenceTransformer
        
        # โหลด embedding model ถ้ายังไม่มี
        if embedding_model is None:
            embedding_model = SentenceTransformer("minishlab/potion-multilingual-128M", device="cpu")
        
        # สร้าง embedding สำหรับคำถาม
        question_embedding = embedding_model.encode(question, convert_to_numpy=True)
        
        # ใช้ embeddings ที่มีอยู่แล้ว
        content_embedding = np.array(content_embeddings)
        
        # ตรวจสอบว่า dimensions ตรงกัน
        if len(question_embedding) != len(content_embedding):
            logger.warning(f"⚠️ Embedding dimensions ไม่ตรงกัน (question: {len(question_embedding)}, content: {len(content_embedding)})")
            return 0.0
        
        # คำนวณ cosine similarity
        similarity = np.dot(question_embedding, content_embedding) / (
            np.linalg.norm(question_embedding) * np.linalg.norm(content_embedding)
        )
        
        return float(similarity)
        
    except Exception as e:
        logger.warning(f"⚠️ Error calculating similarity: {e}")
        return 0.0


def generate_dataset_from_mongo(
    num_questions_batch: int = 10,
    max_total_questions: int = 200,
    top_k_retrieval: int = 5,
    retrieval_threshold: float = 0.10
) -> List[Dict[str, Any]]:
    """
    สร้าง dataset โดยใช้ workflow ใหม่:
    1. ให้ LLM สร้างคำถาม
    2. ใช้ embedding model เพื่อ retrieve ข้อมูลจาก MongoDB
    3. ใช้ระบบ RAG (ask_question_to_rag) เพื่อสร้างคำตอบจากข้อมูลที่ retrieve มา
    
    **สำคัญ**: ใช้ระบบ RAG จริงๆ (ask_question_to_rag) แทนการสร้างคำตอบเอง
    ระบบ RAG จะทำการ retrieve ข้อมูลเองและสร้างคำตอบจากข้อมูลที่ retrieve มา
    
    Args:
        num_questions_batch: จำนวนคำถามที่สร้างต่อ batch
        max_total_questions: จำนวนคำถามสูงสุดทั้งหมด
        top_k_retrieval: จำนวนเอกสารที่ retrieve ต่อคำถาม
        retrieval_threshold: threshold สำหรับ similarity score
        
    Returns:
        List[Dict]: Dataset สำหรับ RAGAS
    """
    # ตรวจสอบ OpenAI API key
    openai_key = os.getenv("OPENAI_API_KEY")
    if not openai_key or openai_key == "your-openai-api-key-here":
        logger.error("❌ OPENAI_API_KEY ไม่ได้ตั้งค่าหรือยังเป็นค่า default")
        return []
    
    openai_client = OpenAI(api_key=openai_key)
    
    # โหลด embedding model สำหรับ retrieval
    try:
        from sentence_transformers import SentenceTransformer
        embedding_model = SentenceTransformer("minishlab/potion-multilingual-128M", device="cpu")
        logger.info("✅ โหลด embedding model สำหรับ retrieval สำเร็จ")
    except Exception as e:
        logger.error(f"❌ ไม่สามารถโหลด embedding model ได้: {e}")
        return []
    
    # วลีที่บ่งชี้ว่าข้อมูลไม่มีในเนื้อหา
    invalid_answer_phrases = [
        "ไม่มีในเนื้อหา", "ไม่มีให้ในเนื้อหา", "ไม่มีข้อมูล", "ไม่พบข้อมูล",
        "ไม่ได้ถูกระบุไว้ในเนื้อหา", "ไม่ได้ระบุในเนื้อหา", "ไม่ได้ถูกระบุในเนื้อหา",
        "ไม่ปรากฏในเนื้อหา", "ยังไม่มีการระบุ", "ไม่มีการระบุ", "ไม่ได้ระบุไว้",
        "ไม่พบในเนื้อหา", "ไม่มีให้ในเนื้อหาที่ให้มา", "ไม่ได้ระบุไว้ในเนื้อหาที่ให้มา",
        "ไม่สามารถให้ได้จากเนื้อหาที่มีอยู่", "ยังไม่ได้มีการระบุ", "ไม่สามารถให้ได้",
        "ไม่สามารถระบุ", "ไม่สามารถระบุได้", "ไม่สามารถระบุจาก", "ไม่สามารถระบุจากข้อมูล",
        "ไม่สามารถระบุจากข้อมูลที่มีอยู่", "ไม่สามารถระบุการคำนวณ", "ไม่สามารถระบุราศี",
        "ไม่สามารถระบุลัคณา", "ไม่สามารถระบุการคำนวณลัคณา", "ไม่สามารถระบุการคำนวณราศี"
    ]
    
    dataset = []
    seen_questions = set()  # เก็บคำถามที่เห็นแล้วเพื่อหลีกเลี่ยงความซ้ำซ้อน
    
    logger.info("🔄 เริ่มสร้าง dataset ด้วย workflow ใหม่:")
    logger.info("   1. ให้ LLM สร้างคำถาม")
    logger.info("   2. ใช้ embedding model เพื่อ retrieve ข้อมูลจาก MongoDB")
    logger.info("   3. ใช้ระบบ RAG (ask_question_to_rag) เพื่อสร้างคำตอบจากข้อมูลที่ retrieve มา")
    logger.info("="*60)
    
    batch_num = 0
    while len(dataset) < max_total_questions:
        batch_num += 1
        remaining_questions = max_total_questions - len(dataset)
        current_batch_size = min(num_questions_batch, remaining_questions)
        
        logger.info(f"\n📦 Batch {batch_num}: สร้าง {current_batch_size} คำถาม")
        
        # 1. ให้ LLM สร้างคำถาม
        logger.info("🔄 ขั้นตอนที่ 1: ให้ LLM สร้างคำถาม...")
        questions = generate_questions_with_llm(
            openai_client=openai_client,
            num_questions=current_batch_size
        )
        
        if not questions:
            logger.warning("⚠️ ไม่สามารถสร้างคำถามได้ - ข้าม batch นี้")
            continue
        
        logger.info(f"✅ สร้างคำถามได้ {len(questions)} คำถาม")
        
        # 2. สำหรับแต่ละคำถาม: retrieve ข้อมูลและสร้างคำตอบ
        for i, question in enumerate(questions, 1):
            if len(dataset) >= max_total_questions:
                logger.info(f"✅ ถึงจำนวนคำถามสูงสุดแล้ว: {max_total_questions}")
                break
            
            # ตรวจสอบว่าคำถามซ้ำหรือไม่
            question_normalized = question.lower().strip()
            if question_normalized in seen_questions:
                logger.warning(f"⚠️ ข้ามคำถามที่ซ้ำ: {question[:50]}...")
                continue
            seen_questions.add(question_normalized)
            
            logger.info(f"\n📝 คำถาม {i}/{len(questions)}: {question[:60]}...")
            
            # 2. ใช้ embedding model เพื่อ retrieve ข้อมูลจาก MongoDB
            logger.info("   🔄 ขั้นตอนที่ 2: Retrieve ข้อมูลจาก MongoDB...")
            retrieved_docs = retrieve_from_mongodb_with_embedding(
                question=question,
                embedding_model=embedding_model,
                top_k=top_k_retrieval,
                threshold=retrieval_threshold
            )
            
            if not retrieved_docs:
                logger.warning(f"   ⚠️ ไม่พบข้อมูลที่เกี่ยวข้อง - ข้ามคำถามนี้")
                continue
            
            logger.info(f"   ✅ Retrieve ได้ {len(retrieved_docs)} เอกสาร")
            if retrieved_docs:
                max_similarity = max(doc.get("similarity", 0.0) for doc in retrieved_docs)
                logger.info(f"   📊 Similarity สูงสุด: {max_similarity:.4f}")
            
            # 3. ใช้ระบบ RAG จริงๆ เพื่อสร้างคำตอบจากข้อมูลที่ retrieve มา
            logger.info("   🔄 ขั้นตอนที่ 3: ใช้ระบบ RAG (ask_question_to_rag) เพื่อสร้างคำตอบ...")
            answer_result = generate_answer_from_retrieved_data(
                question=question,
                retrieved_docs=retrieved_docs,
                invalid_answer_phrases=invalid_answer_phrases
            )
            
            answer = answer_result.get("answer", "").strip()
            contexts = answer_result.get("contexts", [])
            
            # ตรวจสอบว่าคำตอบไม่ว่างเปล่าและไม่มีวลีที่บอกว่าไม่มีข้อมูล
            if not answer:
                logger.warning(f"   ⚠️ ไม่สามารถสร้างคำตอบได้ - ข้ามคำถามนี้")
                continue
            
            if any(phrase in answer for phrase in invalid_answer_phrases):
                logger.warning(f"   ⚠️ คำตอบบอกว่าไม่มีข้อมูล - ข้ามคำถามนี้")
                continue
            
            # ตรวจสอบว่ามี contexts หรือไม่
            if not contexts or not any(ctx.strip() for ctx in contexts if ctx):
                # ใช้เนื้อหาจาก retrieved docs เป็น contexts
                contexts = [doc.get("content", "")[:500] for doc in retrieved_docs[:3] if doc.get("content", "").strip()]
            
            # ใช้ข้อมูลจากเอกสารที่ retrieve มา (ใช้เอกสารที่มี similarity สูงสุด)
            best_doc = retrieved_docs[0] if retrieved_docs else {}
            
            dataset.append({
                "question": question,
                "ground_truth": answer,
                "contexts": contexts,
                "source": best_doc.get("source", "unknown"),
                "content_type": best_doc.get("collection", "unknown").replace("processed_", "").replace("_chunks", ""),
                "page": best_doc.get("page", None),
                "similarity": best_doc.get("similarity", 0.0),
                "_mongodb_id": best_doc.get("_mongodb_id", "unknown"),
            })
            
            logger.info(f"   ✅ สร้างคำถาม-คำตอบสำเร็จ (รวมทั้งหมด: {len(dataset)} คำถาม)")
        
        logger.info(f"\n✅ Batch {batch_num} เสร็จสิ้น: รวมทั้งหมด {len(dataset)} คำถาม")
    
    # ตรวจสอบสุดท้ายว่ามีคำตอบที่บอกว่าไม่มีข้อมูลหรือไม่
    invalid_count = 0
    low_similarity_count = 0
    
    for item in dataset:
        answer = str(item.get("ground_truth", "")).strip()
        
        # ตรวจสอบว่าคำตอบบอกว่าไม่มีข้อมูล
        if any(phrase in answer for phrase in invalid_answer_phrases):
            invalid_count += 1
            similarity = item.get("similarity", 0.0)
            page = item.get("page", "unknown")
            mongo_id = item.get("_mongodb_id", "unknown")
            logger.warning(f"⚠️ พบคำตอบที่บอกว่าไม่มีข้อมูล:")
            logger.warning(f"   คำถาม: {item.get('question', '')[:60]}...")
            logger.warning(f"   คำตอบ: {answer[:80]}...")
            logger.warning(f"   MongoDB: page={page}, _id={mongo_id[:20] if mongo_id != 'unknown' else 'unknown'}, similarity={similarity:.4f}")
        
        # ตรวจสอบ similarity ต่ำ
        similarity = item.get("similarity", 0.0)
        if similarity < retrieval_threshold:
            low_similarity_count += 1
    
    if invalid_count > 0:
        logger.warning(f"\n⚠️ พบคำตอบที่บอกว่าไม่มีข้อมูล {invalid_count} รายการ จากทั้งหมด {len(dataset)} รายการ")
        logger.warning("   ระบบจะกรองคำตอบเหล่านี้ออก")
        # กรองคำตอบที่บอกว่าไม่มีข้อมูลออก
        dataset = [item for item in dataset if not any(phrase in str(item.get("ground_truth", "")).strip() for phrase in invalid_answer_phrases)]
        logger.info(f"✅ กรองคำตอบที่บอกว่าไม่มีข้อมูลออกแล้ว เหลือ {len(dataset)} คำถาม-คำตอบ")
    else:
        logger.info(f"✅ ไม่พบคำตอบที่บอกว่าไม่มีข้อมูล - ทุกคำถาม-คำตอบใช้ข้อมูลจาก MongoDB")
    
    # สรุปสถิติ similarity
    if low_similarity_count > 0:
        logger.warning(f"\n⚠️ พบคำถามที่มี similarity ต่ำมาก (< {retrieval_threshold}) {low_similarity_count} รายการ")
        logger.warning("   อาจเป็นเพราะเนื้อหาจาก MongoDB ไม่เกี่ยวข้องกับคำถาม")
    
    # สรุปข้อมูล source
    source_stats = {}
    for item in dataset:
        source = item.get("source", "unknown")
        source_stats[source] = source_stats.get(source, 0) + 1
    
    logger.info(f"\n📊 สรุปข้อมูล source:")
    for source, count in source_stats.items():
        logger.info(f"   {source}: {count} คำถาม")
    
    logger.info(f"\n✅ สร้าง dataset สำเร็จ: {len(dataset)} คำถาม-คำตอบ")
    return dataset


def save_dataset(dataset: List[Dict[str, Any]], output_file: str = "dataset_from_mongo.json"):
    """
    บันทึก dataset ลงไฟล์ JSON
    
    Args:
        dataset: Dataset ที่จะบันทึก
        output_file: ไฟล์ที่จะบันทึก
    """
    try:
        # เตรียมข้อมูลสำหรับบันทึก (ลบ source และ content_type ออกเพื่อให้เหมือนกับ dataset.json เดิม)
        output_data = []
        for item in dataset:
            output_data.append({
                "question": item["question"],
                "ground_truth": item["ground_truth"],
                "contexts": item["contexts"],
            })
        
        with open(output_file, 'w', encoding='utf-8') as f:
            json.dump(output_data, f, ensure_ascii=False, indent=2)
        
        logger.info(f"✅ บันทึก dataset ลง {output_file} สำเร็จ ({len(output_data)} items)")
        
        # แสดงตัวอย่าง
        if output_data:
            logger.info("\n📋 ตัวอย่างคำถาม-คำตอบ:")
            for i, item in enumerate(output_data[:3], 1):
                logger.info(f"\n{i}. คำถาม: {item['question']}")
                logger.info(f"   คำตอบ: {item['ground_truth'][:100]}...")
        
    except Exception as e:
        logger.error(f"❌ Error saving dataset: {e}")
        import traceback
        traceback.print_exc()


def save_dataset_to_google_sheets(
    dataset: List[Dict[str, Any]],
    spreadsheet_id: Optional[str] = None,
    worksheet_name: str = "Dataset"
) -> bool:
    """
    บันทึก dataset ลง Google Sheets
    
    Args:
        dataset: Dataset ที่จะบันทึก
        spreadsheet_id: ID ของ Google Spreadsheet (ถ้า None จะใช้จาก GOOGLE_SHEETS_ID)
        worksheet_name: ชื่อ worksheet ที่จะบันทึก
        
    Returns:
        bool: True ถ้าส่งสำเร็จ, False ถ้าส่งไม่สำเร็จ
    """
    try:
        import gspread
        from google.oauth2.service_account import Credentials
        
        # ตรวจสอบว่ามีข้อมูลหรือไม่
        if not dataset or len(dataset) == 0:
            logger.warning("⚠️ ไม่มีข้อมูลที่จะบันทึก (dataset ว่าง)")
            return False
        
        # ตรวจสอบ credentials path
        credentials_path = os.getenv("GOOGLE_SHEETS_CREDENTIALS_PATH")
        if not credentials_path or not os.path.exists(credentials_path):
            logger.error(f"❌ ไม่พบ GOOGLE_SHEETS_CREDENTIALS_PATH: {credentials_path}")
            logger.error("   ตรวจสอบว่าไฟล์ credentials มีอยู่และ path ถูกต้อง")
            return False
        
        # โหลด credentials
        creds = Credentials.from_service_account_file(
            credentials_path,
            scopes=['https://www.googleapis.com/auth/spreadsheets', 'https://www.googleapis.com/auth/drive']
        )
        client = gspread.authorize(creds)
        
        # ตรวจสอบ spreadsheet_id
        if spreadsheet_id is None:
            spreadsheet_id = os.getenv("GOOGLE_SHEETS_ID")
        
        if not spreadsheet_id:
            logger.error("❌ ไม่พบ GOOGLE_SHEETS_ID ใน environment variables")
            logger.error("   ตั้งค่า GOOGLE_SHEETS_ID ใน .env")
            return False
        
        # แยก Spreadsheet ID จาก URL (ถ้ามี)
        if "/d/" in spreadsheet_id:
            parts = spreadsheet_id.split("/d/")
            if len(parts) > 1:
                spreadsheet_id = parts[1].split("/")[0].split("?")[0].split("#")[0]
        
        logger.info(f"📊 กำลังบันทึก dataset ลง Google Sheets: {spreadsheet_id}")
        
        # เปิด spreadsheet
        spreadsheet = client.open_by_key(spreadsheet_id)
        
        # ตรวจสอบว่า worksheet มีอยู่หรือไม่ ถ้าไม่มีให้สร้างใหม่
        try:
            worksheet = spreadsheet.worksheet(worksheet_name)
            logger.info(f"✅ พบ worksheet: {worksheet_name}")
            # ล้างข้อมูลเก่า
            worksheet.clear()
            logger.info("🗑️ ล้างข้อมูลเก่าใน worksheet")
        except gspread.exceptions.WorksheetNotFound:
            worksheet = spreadsheet.add_worksheet(title=worksheet_name, rows=1000, cols=10)
            logger.info(f"✅ สร้าง worksheet ใหม่: {worksheet_name}")
        
        # เตรียมข้อมูลสำหรับบันทึก (ลบ Similarity และ MongoDB ID ตามที่ผู้ใช้ขอ)
        headers = [
            "คำถาม",
            "คำตอบ (Ground Truth)",
            "หน้า",
            "ประเภท",
            "Contexts"
        ]
        
        rows = []
        for item in dataset:
            contexts_str = " | ".join(item.get("contexts", []))[:500] if item.get("contexts") else ""
            row = [
                item.get("question", ""),
                item.get("ground_truth", ""),
                item.get("page", "ไม่ระบุ"),
                item.get("content_type", "unknown"),
                contexts_str
            ]
            rows.append(row)
        
        # บันทึก headers
        worksheet.update(values=[headers], range_name='A1:E1')
        
        # บันทึกข้อมูล
        if rows:
            worksheet.update(values=rows, range_name=f'A2:E{len(rows)+1}')
            logger.info(f"✅ บันทึกข้อมูล {len(rows)} rows ลง Google Sheets")
        
        # Format header row
        worksheet.format('A1:E1', {
            'backgroundColor': {'red': 0.2, 'green': 0.4, 'blue': 0.8},
            'textFormat': {'bold': True, 'foregroundColor': {'red': 1.0, 'green': 1.0, 'blue': 1.0}}
        })
        
        logger.info(f"✅ บันทึก dataset ลง Google Sheets สำเร็จ!")
        logger.info(f"📊 Spreadsheet: {spreadsheet.url}")
        return True
        
    except ImportError:
        logger.error("❌ ไม่พบ gspread library")
        logger.error("   ติดตั้งด้วย: pip install gspread google-auth")
        return False
    except Exception as e:
        logger.error(f"❌ Error saving to Google Sheets: {e}")
        logger.error("   ตรวจสอบ logs ด้านล่างเพื่อดูรายละเอียด")
        import traceback
        traceback.print_exc()
        return False


def save_dataset_to_sheet(dataset: List[Dict[str, Any]], output_file: str = "dataset_from_mongo.csv"):
    """
    บันทึก dataset ลง CSV/Excel sheet พร้อมคอลัมน์: question, answer, page, type
    
    Args:
        dataset: Dataset ที่จะบันทึก
        output_file: ไฟล์ที่จะบันทึก (รองรับ .csv และ .xlsx)
    """
    try:
        import pandas as pd
        
        # เตรียมข้อมูลสำหรับ sheet (ลบ Similarity และ MongoDB ID ตามที่ผู้ใช้ขอ)
        sheet_data = []
        for item in dataset:
            sheet_data.append({
                "คำถาม": item.get("question", ""),
                "คำตอบ": item.get("ground_truth", ""),
                "หน้า": item.get("page", "ไม่ระบุ"),
                "ประเภท": item.get("content_type", "unknown"),
            })
        
        # สร้าง DataFrame
        df = pd.DataFrame(sheet_data)
        
        # บันทึกตามประเภทไฟล์
        if output_file.endswith('.xlsx'):
            df.to_excel(output_file, index=False, engine='openpyxl')
            logger.info(f"✅ บันทึก dataset ลง Excel {output_file} สำเร็จ ({len(sheet_data)} rows)")
        else:
            # ใช้ CSV เป็น default
            if not output_file.endswith('.csv'):
                output_file = output_file.replace('.json', '.csv')
            df.to_csv(output_file, index=False, encoding='utf-8-sig')  # ใช้ utf-8-sig เพื่อให้ Excel อ่านได้ถูกต้อง
            logger.info(f"✅ บันทึก dataset ลง CSV {output_file} สำเร็จ ({len(sheet_data)} rows)")
        
        # แสดงตัวอย่าง
        if sheet_data:
            logger.info("\n📋 ตัวอย่างข้อมูลใน sheet:")
            for i, item in enumerate(sheet_data[:3], 1):
                logger.info(f"\n{i}. คำถาม: {item['คำถาม']}")
                logger.info(f"   คำตอบ: {item['คำตอบ'][:100]}...")
                logger.info(f"   หน้า: {item['หน้า']}, ประเภท: {item['ประเภท']}")
        
    except ImportError:
        logger.error("❌ ไม่พบ pandas library. กรุณาติดตั้งด้วย: pip install pandas openpyxl")
        # Fallback: บันทึกเป็น CSV แบบธรรมดา (ลบ Similarity และ MongoDB ID ตามที่ผู้ใช้ขอ)
        try:
            import csv
            with open(output_file.replace('.xlsx', '.csv'), 'w', encoding='utf-8-sig', newline='') as f:
                writer = csv.DictWriter(f, fieldnames=['คำถาม', 'คำตอบ', 'หน้า', 'ประเภท'])
                writer.writeheader()
                for item in dataset:
                    writer.writerow({
                        'คำถาม': item.get("question", ""),
                        'คำตอบ': item.get("ground_truth", ""),
                        'หน้า': item.get("page", "ไม่ระบุ"),
                        'ประเภท': item.get("content_type", "unknown"),
                    })
            logger.info(f"✅ บันทึก dataset ลง CSV {output_file.replace('.xlsx', '.csv')} สำเร็จ (ใช้ csv module)")
        except Exception as e:
            logger.error(f"❌ Error saving dataset to CSV: {e}")
            import traceback
            traceback.print_exc()
    except Exception as e:
        logger.error(f"❌ Error saving dataset to sheet: {e}")
        import traceback
        traceback.print_exc()


def main():
    """
    ฟังก์ชันหลัก - สร้าง RAGAS Dataset โดยใช้ workflow ใหม่:
    1. ให้ LLM สร้างคำถาม
    2. ใช้ embedding model เพื่อ retrieve ข้อมูลจาก MongoDB
    3. ให้ LLM สร้างคำตอบจากข้อมูลที่ retrieve มา
    """
    logger.info("🚀 เริ่มสร้าง RAGAS Dataset ด้วย workflow ใหม่")
    logger.info("📌 Workflow: LLM สร้างคำถาม → Embedding retrieve → LLM สร้างคำตอบ")
    logger.info("="*60)
    
    # พารามิเตอร์
    num_questions_batch = int(os.getenv("NUM_QUESTIONS_BATCH", "10"))
    max_total_questions = int(os.getenv("MAX_TOTAL_QUESTIONS", "200"))
    top_k_retrieval = int(os.getenv("TOP_K_RETRIEVAL", "5"))
    retrieval_threshold = float(os.getenv("RETRIEVAL_THRESHOLD", "0.10"))
    output_file = os.getenv("OUTPUT_DATASET_FILE", "dataset_from_mongo.json")
    output_sheet_file = os.getenv("OUTPUT_SHEET_FILE", "dataset_from_mongo.csv")
    
    logger.info(f"📊 พารามิเตอร์:")
    logger.info(f"   - จำนวนคำถามต่อ batch: {num_questions_batch}")
    logger.info(f"   - จำนวนคำถามสูงสุดทั้งหมด: {max_total_questions}")
    logger.info(f"   - จำนวนเอกสารที่ retrieve ต่อคำถาม: {top_k_retrieval}")
    logger.info(f"   - Threshold สำหรับ similarity: {retrieval_threshold}")
    logger.info(f"   - ไฟล์ผลลัพธ์ JSON: {output_file}")
    logger.info(f"   - ไฟล์ผลลัพธ์ Sheet: {output_sheet_file}")
    logger.info("="*60)
    
    # สร้าง dataset
    dataset = generate_dataset_from_mongo(
        num_questions_batch=num_questions_batch,
        max_total_questions=max_total_questions,
        top_k_retrieval=top_k_retrieval,
        retrieval_threshold=retrieval_threshold
    )
    
    if not dataset:
        logger.error("❌ ไม่สามารถสร้าง dataset ได้")
        return
    
    # บันทึก dataset เป็น JSON
    save_dataset(dataset, output_file)
    
    # บันทึก dataset เป็น Sheet (CSV/Excel)
    save_dataset_to_sheet(dataset, output_sheet_file)
    
    # บันทึก dataset ลง Google Sheets (ถ้าเปิดใช้งาน)
    google_sheets_enabled = os.getenv("GOOGLE_SHEETS_ENABLED", "false").lower() == "true"
    if google_sheets_enabled:
        logger.info("\n📊 กำลังบันทึก dataset ลง Google Sheets...")
        logger.info(f"   จำนวนข้อมูลที่จะบันทึก: {len(dataset)} items")
        success = save_dataset_to_google_sheets(dataset, worksheet_name="Dataset")
        if success:
            logger.info("✅ บันทึก dataset ลง Google Sheets สำเร็จ!")
        else:
            logger.error("❌ ไม่สามารถบันทึก dataset ลง Google Sheets ได้")
            logger.info("💡 ตรวจสอบ logs ด้านบนเพื่อดูรายละเอียด error")
    else:
        logger.info("\n💡 ต้องการบันทึกลง Google Sheets? ตั้งค่า GOOGLE_SHEETS_ENABLED=true ใน .env")
    
    logger.info("\n✅ การสร้าง dataset เสร็จสิ้น!")
    logger.info(f"📁 ไฟล์ผลลัพธ์ JSON: {output_file}")
    logger.info(f"📁 ไฟล์ผลลัพธ์ Sheet: {output_sheet_file}")
    logger.info(f"📊 จำนวนคำถาม-คำตอบ: {len(dataset)}")


if __name__ == "__main__":
    main()

