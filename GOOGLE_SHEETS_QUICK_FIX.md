# 🚀 แก้ไขปัญหา Google Sheets Connection - Quick Fix

## ❌ ปัญหาที่พบ

```
APIError: [403]: Google Sheets API has not been used in project 727945824572 before or it is disabled
```

## ✅ วิธีแก้ไข (ทำ 2 ขั้นตอน)

### ขั้นตอนที่ 1: เปิดใช้งาน Google Sheets API

**⚡ คลิกลิงก์นี้แล้วกด Enable:**
https://console.developers.google.com/apis/api/sheets.googleapis.com/overview?project=727945824572

**หรือ:**

1. ไปที่: https://console.cloud.google.com/apis/library/sheets.googleapis.com
2. เลือกโปรเจกต์ที่มี project number: `727945824572`
3. คลิก **Enable**
4. รอ 1-2 นาที

---

### ขั้นตอนที่ 2: แชร์ Spreadsheet กับ Service Account

1. **เปิด Spreadsheet:**
   https://docs.google.com/spreadsheets/d/1xDMHBIfgLVK-ORdVCSrK4K2MoBXXT0vBhTOYRqXhCFE/edit

2. **คลิกปุ่ม "Share"** (มุมขวาบน)

3. **เพิ่ม email นี้:**
   ```
   ragas-evaluation@ragas-480809.iam.gserviceaccount.com
   ```

4. **ตั้งสิทธิ์เป็น:** `Editor`

5. **คลิก "Send"**

---

## 🧪 ทดสอบการเชื่อมต่อ

หลังจากทำทั้ง 2 ขั้นตอนแล้ว รอ 1-2 นาที แล้วรัน:

```bash
python3 test_google_sheets_connection.py
```

ถ้าทำถูกต้องจะเห็น:
```
✅ เปิด Spreadsheet สำเร็จ: [ชื่อ Spreadsheet]
✅ ทดสอบเขียนข้อมูลสำเร็จ
✅ การเชื่อมต่อ Google Sheets ทำงานได้ปกติ!
```

---

## 📝 Checklist

- [ ] เปิดใช้งาน Google Sheets API แล้ว
- [ ] แชร์ Spreadsheet กับ Service Account แล้ว
- [ ] ตั้งสิทธิ์เป็น Editor แล้ว
- [ ] รอ 1-2 นาที แล้วทดสอบอีกครั้ง

---

## 🔍 ตรวจสอบว่าเปิดใช้งาน API แล้วหรือยัง

1. ไปที่: https://console.cloud.google.com/apis/library/sheets.googleapis.com?project=727945824572
2. ดูว่ามีปุ่ม "Enable" หรือ "Manage"
   - ถ้ามี "Enable" = ยังไม่ได้เปิดใช้งาน → คลิก Enable
   - ถ้ามี "Manage" = เปิดใช้งานแล้ว ✅

---

## 🔍 ตรวจสอบว่าแชร์ Spreadsheet แล้วหรือยัง

1. เปิด Spreadsheet
2. คลิก "Share"
3. ดูว่ามี email `ragas-evaluation@ragas-480809.iam.gserviceaccount.com` ในรายการหรือไม่
   - ถ้าไม่มี = ยังไม่ได้แชร์ → เพิ่ม email
   - ถ้ามี = แชร์แล้ว ✅
