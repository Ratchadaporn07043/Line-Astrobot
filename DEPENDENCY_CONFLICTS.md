# Dependency Conflicts และวิธีแก้ไข

## ⚠️ ปัญหาที่พบ

### 1. pypdf Version Conflict

**ปัญหา:**
- `camelot-py` ต้องการ `pypdf<4.0,>=3.17`
- `unstructured-client` ต้องการ `pypdf>=4.0`

**ผลกระทบ:**
- ไม่สามารถใช้ทั้งสองแพ็กเกจพร้อมกันได้โดยไม่มี conflict

**วิธีแก้ไข:**

#### ตัวเลือกที่ 1: ใช้ camelot-py (แนะนำ)
```bash
pip install "pypdf>=3.17.0,<4.0"
```
- ✅ camelot-py ทำงานได้
- ⚠️ unstructured-client อาจมีปัญหา (แต่ยังไม่แน่ใจว่าจำเป็นต้องใช้หรือไม่)

#### ตัวเลือกที่ 2: ใช้ unstructured-client แทน
```bash
pip install "pypdf>=4.0.0"
```
- ✅ unstructured-client ทำงานได้
- ⚠️ camelot-py จะไม่ทำงาน (แต่มี pdfplumber เป็น fallback)

### 2. pdfminer.six Version

**ปัญหา:**
- `pdfplumber` ต้องการ `pdfminer.six==20221105`
- `camelot-py` ติดตั้ง `pdfminer.six==20251107` (ใหม่กว่า)

**วิธีแก้ไข:**
```bash
pip install "pdfminer.six>=20221105,<20260000"
```
- รองรับทั้ง pdfplumber และ camelot-py

### 3. Ghostscript ไม่พบ

**ปัญหา:**
- Camelot ต้องการ Ghostscript แต่ยังไม่ได้ติดตั้ง

**วิธีแก้ไข (macOS):**
```bash
brew install ghostscript
mkdir -p ~/lib
ln -s "$(brew --prefix gs)/lib/libgs.dylib" ~/lib
```

---

## ✅ คำแนะนำ

สำหรับโปรเจคนี้:
1. **ใช้ camelot-py** สำหรับ extract ตาราง (แม่นยำกว่า)
2. **ใช้ pypdf 3.17.x** เพื่อให้ camelot-py ทำงานได้
3. **ติดตั้ง Ghostscript** เพื่อให้ camelot-py ทำงานเต็มประสิทธิภาพ
4. **ใช้ pdfplumber เป็น fallback** ถ้า camelot-py ไม่ได้

---

## 📝 คำสั่งติดตั้งที่แนะนำ

```bash
# 1. ติดตั้ง Ghostscript
brew install ghostscript
mkdir -p ~/lib
ln -s "$(brew --prefix gs)/lib/libgs.dylib" ~/lib

# 2. ติดตั้ง dependencies ที่เข้ากันได้
pip install "pypdf>=3.17.0,<4.0"
pip install "pdfminer.six>=20221105,<20260000"
pip install "camelot-py[cv]"

# 3. ทดสอบ
python3 test_camelot.py
```

---

## 🔍 ตรวจสอบ Dependencies

```bash
pip list | grep -E "pdfminer|pypdf|camelot"
```

ควรเห็น:
- `pdfminer.six` >= 20221105
- `pypdf` >= 3.17.0, < 4.0
- `camelot-py` >= 0.11.0
