# ระบบจำกัดคำถามต่อเนื่อง 3 คำถามต่อวัน

## ภาพรวม
ระบบนี้จำกัดการถามคำถามต่อเนื่องของผู้ใช้ให้ไม่เกิน 3 คำถามต่อวัน เพื่อควบคุมการใช้งานและประหยัดทรัพยากร

## ฟีเจอร์ที่เพิ่มเข้ามา

### 1. ระบบติดตามจำนวนคำถาม
- **ฟังก์ชัน**: `check_and_update_question_limit(user_id, max_questions=3)`
- **ตำแหน่ง**: `app/retrieval_utils.py`
- **หน้าที่**: ตรวจสอบและอัปเดตจำนวนคำถามของผู้ใช้

### 2. การรีเซ็ตจำนวนคำถาม
- **ระบบ**: รีเซ็ตทุกวันใหม่ (00:00 UTC)
- **การทำงาน**: ตรวจสอบวันที่ของคำถามล่าสุดและรีเซ็ตถ้าเป็นวันใหม่

### 3. การแจ้งเตือนผู้ใช้
- **คำถามที่ 1-2**: แสดงจำนวนคำถามที่เหลือ
- **คำถามที่ 3**: แจ้งว่าเป็นคำถามสุดท้าย
- **คำถามที่ 4+**: แจ้งว่าเกินขีดจำกัดและต้องรอวันใหม่

## การทำงานของระบบ

### ขั้นตอนการตรวจสอบ
1. ตรวจสอบความปลอดภัยของเนื้อหา
2. ตรวจสอบ/สร้างโปรไฟล์ผู้ใช้
3. **ตรวจสอบจำนวนคำถามต่อเนื่อง** ← ฟีเจอร์ใหม่
4. ประมวลผลคำถามผ่าน RAG

### ข้อความแจ้งเตือน
```
คำถามที่ 1-2: "💫 คุณสามารถถามคำถามได้อีก X คำถามในวันนี้"
คำถามที่ 3: "⚠️ นี่เป็นคำถามสุดท้ายของคุณในวันนี้"
คำถามที่ 4+: "ขออภัยครับ คุณได้ถามคำถามครบ 3 คำถามแล้วในวันนี้..."
```

## ไฟล์ที่แก้ไข

### 1. `app/retrieval_utils.py`
- เพิ่มฟังก์ชัน `check_and_update_question_limit()`
- อัปเดตฟังก์ชัน `get_user_context()` เพื่อรวมข้อมูลจำนวนคำถาม
- แก้ไขฟังก์ชัน `ask_question_to_rag()` เพื่อตรวจสอบขีดจำกัด

### 2. `app/response_message.py`
- เพิ่มการตรวจสอบขีดจำกัดใน `generate_reply_message()`
- เพิ่มการบันทึกข้อมูลเมื่อเกินขีดจำกัด

### 3. `app/main.py`
- เพิ่มการตรวจสอบขีดจำกัดใน API endpoint `/ask`

## ข้อมูลที่เก็บในฐานข้อมูล

### Collection: `user_profiles`
```json
{
  "user_id": "string",
  "daily_question_count": 0,
  "last_question_date": "2024-01-01",
  "birth_date": "string",
  "created_at": "datetime",
  "updated_at": "datetime"
}
```

### Collection: `responses`
```json
{
  "user_id": "string",
  "question": "string",
  "answer": "string",
  "response_type": "question_limit_exceeded",
  "context_data": {
    "question_count": 3,
    "max_questions": 3
  },
  "created_at": "datetime"
}
```

## การทดสอบ

### ไฟล์ทดสอบ: `test_question_limit.py`
```bash
python test_question_limit.py
```

### สถานการณ์ทดสอบ
1. คำถามแรก - อนุญาต
2. คำถามที่สอง - อนุญาต
3. คำถามที่สาม - อนุญาต (คำถามสุดท้าย)
4. คำถามที่สี่ - ปฏิเสธ

## การตั้งค่า

### จำนวนคำถามสูงสุด
- **ค่าเริ่มต้น**: 3 คำถามต่อวัน
- **การเปลี่ยน**: แก้ไขพารามิเตอร์ `max_questions` ในฟังก์ชัน `check_and_update_question_limit()`

### การรีเซ็ต
- **แบบ**: รีเซ็ตทุกวันใหม่
- **เวลา**: 00:00 UTC
- **การเปลี่ยน**: แก้ไขในฟังก์ชัน `check_and_update_question_limit()`

## ข้อดีของระบบ
1. **ควบคุมการใช้งาน**: ป้องกันการใช้งานมากเกินไป
2. **ประหยัดทรัพยากร**: ลดการใช้ API และการประมวลผล
3. **ประสบการณ์ผู้ใช้**: แจ้งเตือนชัดเจนเกี่ยวกับขีดจำกัด
4. **ยืดหยุ่น**: สามารถปรับจำนวนคำถามได้ง่าย

## ข้อควรระวัง
1. ระบบจะอนุญาตให้ถามได้ในกรณีที่เกิดข้อผิดพลาด
2. การรีเซ็ตใช้เวลาตาม UTC ไม่ใช่เวลาท้องถิ่น
3. ข้อมูลจำนวนคำถามจะถูกเก็บในฐานข้อมูล MongoDB
