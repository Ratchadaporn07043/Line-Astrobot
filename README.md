# AstroBot

ระบบแชทบอทโหราศาสตร์อัจฉริยะที่ผสมผสานศาสตร์แห่งการพยากรณ์เข้ากับเทคโนโลยี AI (RAG - Retrieval Augmented Generation) เพื่อให้คำทำนายที่แม่นยำและเป็นส่วนตัวผ่าน LINE OA

## 🛠 การทำงานของระบบ (System Operation)

ระบบ AstroBot มีกระบวนการทำงานหลัก 4 ขั้นตอน ดังนี้:

### 1. การรับและวิเคราะห์ข้อความ (Input Processing)
**ไฟล์ที่เกี่ยวข้อง:** `app/birth_date_parser.py`
- ระบบรับข้อความจากผู้ใช้ผ่าน LINE Webhook (`app/main.py`)
- ใช้ Natural Language Processing (NLP) เพื่อวิเคราะห์เจตนาของผู้ใช้ (Intent Detection)
- ตรวจสอบและแยกข้อมูลวันเกิด เวลาเกิด และสถานที่เกิด (ถ้ามี) จากข้อความสนทนา เพื่อนำไปใช้ในการผูกดวง
- หากพบข้อมูลวันเกิด จะทำการสร้างหรืออัปเดตโปรไฟล์ผู้ใช้ (`user_profiles`) โดยอัตโนมัติ

### 2. การคำนวณทางโหราศาสตร์ (Astrological Calculation)
**ไฟล์ที่เกี่ยวข้อง:** `app/astronomical_calculator.py`
- ใช้ไลบรารี `flatlib` ในการคำนวณตำแหน่งดาวเคราะห์ (Planetary Positions) อย่างแม่นยำตามหลักดาราศาสตร์
- **การหาลัคนา (Ascendant):** คำนวณลัคนาโดยใช้วัน เวลา และพิกัดสถานที่เกิด (Latitude/Longitude)
- **การวางลัคนา:** คำนวณตำแหน่งบ้าน (House Cusps) ทั้ง 12 ภพ เพื่อดูความสัมพันธ์ของดาวในแต่ละเรื่องราวของชีวิต
- แปลผลตำแหน่งดาวและมุมสัมพันธ์ (Aspects) เบื้องต้น เช่น ดาวเกษตร ดาวอุจจ์ หรือมุมตรีโกณ

### 3. ระบบค้นหาข้อมูล (RAG - Retrieval Augmented Generation)
**ไฟล์ที่เกี่ยวข้อง:** `app/retrieval_utils.py`
- ใช้เทคนิค **Vector Search** เพื่อค้นหาความหมายทางโหราศาสตร์ที่เกี่ยวข้องจากฐานข้อมูล MongoDB (`astrobot_original`)
- ระบบจะค้นหาข้อมูลจาก 3 แหล่งหลัก:
  - **Text Chunks:** คำทำนายจากตำราโหราศาสตร์
  - **Image Chunks:** แผนภาพดวงชะตาที่เกี่ยวข้อง
  - **Table Chunks:** ตารางความสัมพันธ์ของดาว
- มีระบบกรองข้อมูล (Filtering) ตามราศี ดาวเคราะห์ และธาตุ เพื่อให้ได้ข้อมูลที่ตรงกับดวงชะตาของผู้ใช้มากที่สุด

### 4. การสร้างคำตอบ (Response Generation)
**ไฟล์ที่เกี่ยวข้อง:** `app/response_message.py`
- นำข้อมูลดวงชะตาที่คำนวณได้ (`Chart Info`) มารวมกับข้อมูลความหมายที่ค้นหาเจอ (`Retrieved Context`)
- ส่งข้อมูลทั้งหมดให้ **Large Language Model (LLM)** ประมวลผลและเรียบเรียงคำทำนาย
- สร้างคำตอบที่เข้าใจง่าย เป็นธรรมชาติ และตรงกับบริบทคำถาม (เช่น ความรัก การงาน การเงิน)
- ส่งคำตอบกลับไปยังผู้ใช้ผ่าน LINE Messaging APl

---
*เอกสารนี้อัปเดตล่าสุดเมื่อ: 2025-12-26*